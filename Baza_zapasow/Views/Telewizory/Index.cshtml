@model IEnumerable<Baza_zapasow.Models.Telewizory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rozmiar)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rozmiar)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TelewizorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TelewizorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TelewizorID })
        </td>
    </tr>
}

</table>
