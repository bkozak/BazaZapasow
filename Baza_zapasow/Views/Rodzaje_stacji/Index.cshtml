@model IEnumerable<Baza_zapasow.Models.Rodzaje_stacji>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Rodzaj)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Rodzaj)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Rodzaj_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Rodzaj_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Rodzaj_Id })
        </td>
    </tr>
}

</table>
